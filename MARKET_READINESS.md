# Market readiness review

## Current continuation checkpoint (2026-09-24)

Latest code is on `main` at `29ba0737a565a7c50df3b3a7c4c826db091bb3be`. The quality workflow [35978695784](https://github.com/TejasviUpadhyay1907/Lead_Agent/actions/runs/35978695784) and CodeQL [35978695775](https://github.com/TejasviUpadhyay1907/Lead_Agent/actions/runs/35978695775) both passed. Response approval now requires the server to receive `claims_verified: true` as a strict boolean and writes that attestation into the approval audit event; this is a review safeguard only, and does not enable message delivery. The first integration candidate is Zoho CRM + Meta WhatsApp Business Platform for an India-based SMB, subject to design-partner confirmation. No live vendor connector or staging deployment is proven, so market readiness remains pre-staging/pre-pilot.

## Assessment

This repository is a credible prototype with a useful deterministic policy boundary, but it is not ready to sell as an industrial lead-management system. The existing architecture and UI demonstrate a workflow; they do not yet establish secure multi-company operation, reliable integrations, or production service levels. “Perfect” cannot be verified from source code alone: customer identity systems, CRM/communications credentials, deployment configuration, and operational ownership are not present in this workspace.

## Competitive reality and product wedge (2026-09-24)

Lead capture, scoring, follow-up automation, AI assistance, omnichannel engagement, and WhatsApp connectivity are already advertised capabilities of established CRM suites. For example, Zoho describes lead scoring, automated follow-ups, omnichannel engagement, and connecting with prospects over WhatsApp in its [lead-management overview](https://www.zoho.com/crm/lead-management/) and [CRM feature catalog](https://www.zoho.com/crm/features.html). These are vendor descriptions, not an independent feature or quality assessment. They are enough to reject “we have AI lead scoring and reply drafts” as a defensible differentiator by itself.

The product hypothesis worth validating is narrower and outcome-led: **a CRM-adjacent lead-response operations layer that helps SMB sales teams find missed or at-risk inbound inquiries across their existing channels, coordinates a human-approved next action, and proves whether the lead was actually contacted and converted.** The repository has pieces of this workflow (signed inbound webhook, deterministic risk/priority, human approval, follow-up tracking), but lacks vendor sync and delivery receipts, company-wide outcome reporting, measured ROI, and customer evidence. Operator-entered won/lost outcomes now provide a first auditable outcome record; they are not CRM-sourced or conversion attribution. Treat the wedge as a hypothesis until sales users confirm the pain and a pilot measures it.

The next integration should preserve the customer's CRM as the system of record and close one end-to-end loop: source event → safe deduplicated intake → triage → assigned human review → consent-checked provider delivery → provider receipt → CRM update → conversion and response-time reporting. Validate the first CRM/channel with a target customer before implementing a vendor-specific connector; do not position the generic webhook as a completed CRM integration.

### Pilot connector default to validate (2026-09-24)

With an India-based SMB pilot as the working assumption and no customer stack specified, the recommended first connector candidate is **Zoho CRM + Meta WhatsApp Business Platform (Cloud API)**. Treat this as a default to confirm with the design partner, not as a universal market choice. Zoho exposes OAuth 2.0 delegated API access and CRM record webhooks, which fit customer-authorized read/write synchronization; WhatsApp has a hosted developer hub with test numbers, webhooks, a sandbox, and delivery integration material. WhatsApp policy requires permission to message, honoring opt-outs, and approved templates to initiate conversations, so consent provenance, template selection, and delivery status must be first-class workflow gates. See [Zoho OAuth](https://www.zoho.com/crm/developer/docs/api/v8/oauth-overview.html), [Zoho CRM webhooks](https://www.zoho.com/crm/developer/docs/api/v8/create-webhook.html), [WhatsApp Business developer hub](https://whatsappbusiness.com/developers/developer-hub/), and [WhatsApp Business Messaging Policy](https://whatsappbusiness.com/policy/).

Do not build around an assumed customer's WhatsApp number, Zoho account, or consent records. Begin with a sandbox and an explicit field mapping, then verify OAuth scopes, region/data handling, approved message templates, webhook signatures/retries, provider receipts, and CRM update semantics with the selected pilot owner.

## Confirmed gaps

| Area | Current evidence | Required before selling |
|---|---|---|
| Identity and authorization | API routes validate HTTPS-hosted OIDC RS256 bearer tokens, required `exp`/`iat`/`sub`, resource audience (or `client_id` only when `aud` is absent), required scope, tenant claim, and now require operator/admin roles for customer-data reads as well as existing writes. The browser has PKCE sign-in and memory-only token handling; there is no refresh-token lifecycle, and the flow has not been validated against a real provider. | Validate provider discovery/CORS/role claims and audience semantics, test sign-in/out and key rotation, and complete security review. |
| Company separation | Each API deployment validates one tenant claim; lead, follow-up, and audit items now carry that tenant, and list/detail paths enforce it. Contact-history and suppression indexes use keyed HMAC pseudonyms from a dedicated per-deployment Secrets Manager key. The runtime still serves only one tenant per deployment, so shared SaaS orchestration is not implemented. | Keep isolated stacks per customer for the first release; shared SaaS needs per-request tenant repository context and cross-tenant isolation testing. Create and protect the index key, migrate existing indexes and suppressions, verify key-fingerprint markers, and document safe key rotation before customer use. |
| Database reliability | Demo mode can fall back to process memory, which disappears on Lambda recycle. Inbound webhook, manual lead creation, analysis, response approval/edit/rejection, successful rescue, lifecycle status changes, follow-up completion, and company-config updates now atomically write their associated records. State-changing workflows use conditional writes to reject stale updates. SAM enables point-in-time recovery for leads, follow-ups, audit, and config tables. | Keep memory fallback demo-only (now enforced), run restore drills, complete migrations, and establish remaining idempotency and recovery procedures. |
| Scale and query costs | Lead and follow-up pages query tenant-scoped GSIs; audit history queries a tenant-plus-lead GSI and is now cursor-paginated. Pages cap at 100. Lead-list opt-out checks now batch up to 100 contact keys per strongly consistent request, retry unprocessed keys with bounded backoff, and fail closed if reads remain incomplete. Suppression migration readiness is checked lazily on the first readiness/lead operation, avoiding DynamoDB network calls during app import and cold start; registry read outages return HTTP 503. Single-tenant partition throughput still needs load validation. | Establish capacity/cost targets and load tests; assess write/read hot partitions at expected customer volumes. |
| Deploy security | SAM defaults production mode, requires a trusted API origin, OIDC and tenant parameters, scopes DynamoDB operations, and throttles the API stage (default 50 requests/second, burst 100) with a per-deployment worker concurrency cap. Bedrock now requires an explicit geographic inference profile and matching foundation model; the API/worker IAM statements authorize the exact profile and only the matching model, constrained to invocation through that profile. Backend Python dependencies are hash-locked for Python 3.12 Linux x86_64, and the frontend has an npm lockfile. WAF policy, customer-specific encryption/key policy, staging invocation proof, and deployment review remain open. A read-only STS check found no usable AWS credentials in this local environment, so staging could not be inspected or deployed. | Verify profile/model compatibility and IAM in the target AWS region, document and approve the profile's full processing geography, add edge protection, maintain lockfiles, define key/secret and encryption policy, separate environments, and complete deployment/security review. |
| Demo controls | Seed/clock endpoints share the API router but return 404 unless both demo mode and a local/test environment are active. | Keep seed/clock routes unreachable in every deployed environment and validate that behavior in staging. |
| Integrations | A signed, tenant-isolated inbound webhook now accepts lead events with replay protection, 30-day idempotency, and an atomic lead/audit/event transaction. No vendor-specific OAuth/CRM sync, bidirectional updates, messaging, or delivery receipts exist. | Pilot the generic connector with an actual source system; then add vendor OAuth/field mapping, consent sync, delivery status, and connector health. |
| Business configuration | Admin changes are role-gated, audited atomically, and validate SLA, deal threshold, business name, locations, JSON shape, and payload size server-side. Secret-like keys are rejected on new updates; existing secret-like values are masked from API responses and preserved if unrelated settings are saved. | Migrate any credentials previously stored in config into Secrets Manager; validate customer-specific rules and define approval/change-control for high-impact updates. |
| Messaging | Local changes now record approval as `APPROVED` without changing contact lifecycle, and SLA risk only clears for a provider-confirmed `SENT` state. Legacy `SIMULATED_SENT` records are treated as undelivered by risk evaluation and displayed as “Contact unverified” when their lifecycle is `CONTACTED`. No outbound provider exists. Historical records need a reviewed correction before customer use. A read-only inventory script correlates old approvals with lifecycle audit events without changing data. | Run the inventory in authorized staging and manually reconcile the results; verify approval behavior in staging; implement a provider adapter, delivery receipts, idempotency, retry/dead-letter handling, suppression enforcement, and human approval policy. |
| Agent reliability | The browser's analysis flow uses durable SQS jobs with tenant-checked polling, retries, terminal failure state, and a monitored DLQ. The opt-in benchmark has 20 synthetic labeled cases and four literal prompt-injection canaries; it has not been run against Bedrock. The deterministic fallback no longer promises unsupported response timing, specialist review, specs, or pricing; the live prompt prohibits unconfigured commitments and the UI reminds operators to verify claims before approval. That reminder is not a deterministic live-output claim validator. Labels/thresholds remain exploratory and canary success is narrow evidence only. | Broaden representative/adversarial cases, add automated claim-grounding review for live drafts, run the benchmark against the approved staging model, calibrate gates with a design partner, measure per-tenant latency/cost, and validate queue recovery/job states in AWS staging. |

| Privacy/compliance | A tenant-scoped admin queue stores metadata-only access/erasure requests from manual and webhook intake, links them to lead audit events, and provides conditional audited status transitions. A persisted per-lead hold blocks AI and sales actions; erasure requests also use durable contact suppression. After identity is marked verified, admins can page candidate leads matching the request lead's stored email or normalized phone; results minimize PII and each lookup records matched lead IDs in the audit trail. Automated coverage exercises the hold, transitions, discovery gate/pagination, audit, suppression, and representative phrase negatives. Current local backend suite (160 tests), frontend build, and SAM lint validation pass; none has been deployed. Candidate lookup is not a complete person-wide inventory: it misses changed/missing identifiers and unrelated systems, and it does not fulfill subject-wide export/deletion, retention, or legal-hold handling. | Prove indexed discovery and transaction behavior against DynamoDB and deployed tenant roles; expand inventory to connected systems and identity resolution, implement verified subject-wide access/export and controlled erasure, retention/legal-hold handling, fulfillment evidence and deadlines. Create/protect the per-customer index key; run and verify both index and opt-out backfills before cutover; define consent, encryption/key policy, access logs, incident process, threat review, and jurisdiction-specific review. |
| Operations | SAM sets a 90-day default Lambda log retention and now requires an SNS alarm topic, with API/worker error and throttle alarms plus queue-age/DLQ alarms. The template cannot prove that the topic has verified subscribers or an active on-call owner. Dashboards, SLOs, traces, incident ownership, restore drills, and recovery runbooks are not present. | Configure and verify the production SNS topic and on-call delivery, define dashboards/SLOs and owners, add traces and incident procedures, and run restore drills. |
| Product evidence | Lead detail supports operator-entered won/lost/disqualified outcomes; the dashboard now reports a bounded, paginated UTC lead-created cohort with totals, decided-lead win rate, and won values separated by currency. Outcomes remain manually entered, and the underlying GSI is eventually consistent; this is not CRM attribution, time-to-response reporting, or recovered revenue. No measured customer outcomes or service commitments exist. | Validate outcome correctness with CRM data, add response-time and attributable conversion measures, define ICP/onboarding/support, and prove ROI with pilots and honest product boundaries. |
| Frontend design and usability | Browser inspection found that no stylesheet was loaded because the frontend entry point never imported `index.css`; much of the screen UI also lacked matching rules. The entry point and styles are now wired, all current static view classes have styles, mobile/desktop inbox and detail layouts were checked in synthetic mode, and keyboard focus rings were confirmed. The full route-based design-debt register is in [FRONTEND_DESIGN_AUDIT.md](FRONTEND_DESIGN_AUDIT.md). Screenshot-based visual approval, screen-reader/WCAG review, and sales-user task research remain open. | Review real screenshots on target devices; complete keyboard/screen-reader and contrast checks; run user research with sales teams and tune around real CRM data and workflow. |

### Outcome-capture implementation update (2026-09-24)

- Added operator-recorded `won`, `lost`, and `disqualified` outcomes to the lead record. Won outcomes may include a confirmed amount and ISO-style three-letter currency; lost/disqualified outcomes require a reason. Recording an outcome resolves the lead and commits its updated state with an audit event using the repository's optimistic concurrency control.
- Privacy-held and opted-out/suppressed contacts cannot have their outcome updated. Outcome changes are auditable corrections; the audit records previous and new values. The agent never infers the outcome.
- Added a lead-detail form that labels values as manually entered and states that CRM sync and revenue attribution are not established. This is data capture only: there is no company-level aggregate report yet, and manual deal values must not be represented as recovered revenue.
- Frontend `npm run build` passed; the focused API suite passes **12 tests**, and the full local backend suite passes **164 tests** (two third-party deprecation warnings). A repository regression check confirms won deal values remain exact DynamoDB numbers. No staging or CRM behavior has been verified; CodeQL/CI for the implementation commit remain pending publication.

### Company outcome reporting continuation (2026-09-24)

- Added `GET /api/reports/outcomes`, restricted to sales operators/admins. It validates a maximum 365-day UTC date window, rejects future end dates, pages the tenant-scoped created-at index, and reports the cohort definition and DynamoDB eventual-consistency caveat.
- Dashboard supports 7/30/90/365-day windows, explicit paging to a 10,000-lead safety limit, and hides totals when the selected range exceeds that limit. Counts include won/lost/disqualified/open; win rate uses only decided won+lost leads; won amounts remain separated by currency and are manually entered, not attributed revenue.
- Focused report tests pass; full backend suite passes **166 tests** (two upstream deprecation warnings); frontend production build passed; `git diff --check` passed. Published in commit `ae5024416060e29e698e5dc413f6d1b48fb76b89`; GitHub quality [run 35976342364](https://github.com/TejasviUpadhyay1907/Lead_Agent/actions/runs/35976342364) and CodeQL [run 35976342464](https://github.com/TejasviUpadhyay1907/Lead_Agent/actions/runs/35976342464) both passed. No live database, staging deployment, CRM attribution, or customer results were verified.
| Release quality gates | The pull-request/push workflow builds the frontend, audits all locked frontend dependencies at high severity or above, audits the hashed Python runtime lock, verifies the generated backend lock, installs pinned backend/test dependencies, runs backend tests, checks Python syntax, and validates SAM. A separate CodeQL workflow analyzes JavaScript/TypeScript and Python on pull requests, pushes to `main`, and weekly. First CodeQL run found a sensitive logging issue, which was fixed; follow-up run [35922904976](https://github.com/TejasviUpadhyay1907/Lead_Agent/actions/runs/35922904976) passed both languages and GitHub reports zero open code-scanning alerts. Dependency and static analyses do not replace security review or prove deployment safety. GitHub API confirms `main` is currently unprotected, so successful checks are not enforced before updates land. | Decide and configure the merge policy (required PR review/status checks versus preserving direct pushes); keep findings triaged; add cloud integration tests, deployment review, load/restore drills, and staging verification. |

### Opt-out language coverage

Opt-out safety is enforced by deterministic policy at manual lead intake, signed webhook intake, analysis policy evaluation, and downstream suppression checks. The phrase detector now normalizes punctuation, case, and common Unicode apostrophes; covers common English requests to stop contact, unsubscribe, remove/delete contact details, and avoid future messages; and avoids several negated and informational examples. API tests verify opt-out lifecycle state for representative variants and verify an opt-out-policy question does not suppress a lead. This remains a bounded English phrase detector, not semantic or multilingual consent understanding. Before customer use, validate against each selected channel's provider-level unsubscribe/consent events, customer-reviewed phrases, multilingual samples, and false-positive/false-negative cases. Provider consent events must be authoritative and should feed the durable suppression registry.

An opted-out lead is also rejected before either synchronous or queued analysis, preventing a fresh Bedrock request after an opt-out. A regression test covers both API entry points. A job queued before the opt-out is enforced again by the analysis core when the worker executes, so the inquiry is rejected at execution time too.

The same execution-time regression uncovered that `analyze_lead_core` relied on `Lead.last_analysis_job_id` for SQS redelivery idempotency, but the field existed only on `LeadUpdate`. It is now part of the persisted `Lead` schema, so new analysis writes retain the idempotency marker and existing DynamoDB items load with a safe `None` default. Tests cover duplicate-job early return without rerunning inference. This fixes source-level behavior; queue delivery and transactional redelivery still need staging verification.

Direct backend tests now exercise job idempotency-key reuse/mismatch, exclusive claim and retry transitions, expired-lease recovery, worker success, duplicate-delivery skipping, retryable and terminal failures, opt-out failure handling, and DLQ crash handling. These tests validate application state handling with in-memory/fake dependencies; they do not prove AWS SQS/DynamoDB delivery behavior, timing, or IAM in a deployed stack.

## Changes made in this review

- Customer-history lookup now excludes the lead being analyzed before sending the prior-interaction count to Bedrock; the deterministic demo fallback also marks a customer returning when at least one earlier interaction exists. Stored customer name, email, and phone plus common name/email/phone patterns in inquiry text are redacted before Bedrock. The original inquiry remains intact in company storage; uncommon identifier formats and other sensitive content may remain in the model context. Regression coverage verifies the boundary. Broader model-quality evaluation remains open.
- Added a versioned, 20-case synthetic structured-field benchmark at `backend/evals/lead_agent_cases.json`, scorer at `backend/app/agent/evaluation.py`, and Bedrock runner at `backend/scripts/evaluate_lead_agent.py`. Four literal injection canaries now check product/location/entities/summary/recommendation/response text, while reports exclude source messages, generated text, and canary values. The runner still requires explicit confirmation before potentially billed inference, uses in-memory synthetic repositories, and rejects fallback-only outcomes. New scorer/corpus tests are included in the 160-test local suite. Live Bedrock evaluation has not been run; labels and thresholds are exploratory rather than validated release gates, and canaries do not establish general prompt-injection robustness.
- Expanded deterministic opt-out phrase handling for punctuation and common English wording; added negative/informational examples to prevent avoidable suppression. API and policy tests cover representative cases, and both synchronous and queued analysis reject opted-out contacts before model invocation. This reduces a clear lexical gap but is not multilingual, semantic, or provider consent integration; validate with real-channel consent events before customer use.
- Fixed a worker idempotency schema mismatch: `last_analysis_job_id` was writable via `LeadUpdate` but absent on persisted `Lead`, even though worker analysis reads/writes it to suppress duplicate inference after SQS redelivery. The field is now on `Lead`; tests verify execution-time opt-out rejection and completed-job duplicate suppression.
- Added direct worker/job-repository regression coverage for idempotency-key reuse, atomic claim eligibility, retry/lease recovery, duplicate delivery, success, retryable/terminal failures, opt-out failures, and DLQ worker-crash handling. These are code-level tests; actual AWS queue timing, IAM, and redrive remain staging gates.
- Added full npm dependency and hashed Python runtime lock vulnerability audits to the GitHub Actions quality workflow. Local scans found zero frontend vulnerabilities and no known Python vulnerabilities in the lock. GitHub Actions run [35914056333](https://github.com/TejasviUpadhyay1907/Lead_Agent/actions/runs/35914056333) passed both audits and the build/test/SAM checks for the CI-gate commit.
- Added a separately permission-scoped CodeQL workflow for JavaScript/TypeScript and Python on pull requests, pushes to `main`, and a weekly schedule. The workflow uses immutable action SHAs and uploads findings to GitHub code scanning. Its first run found that the customer index-secret helper printed the Secrets Manager ARN; the CLI now omits it, the follow-up scan passes, and GitHub marks the alert fixed. Code scanning is operational at the workflow level; branch protection is still unverified.

- SAM production defaults disable demo behavior (`DEMO_MODE=false`, `APP_ENV=production`); demo clock and seed routes return 404 in all deployed environments.
- FastAPI CORS now uses configured origins and does not permit credentialed wildcard access.
- SAM defaults demo mode off and accepts an explicit trusted API origin; see the Bedrock profile/model IAM change below for current invocation permissions.
- Bedrock deployment now requires an explicit geographic inference profile ID and its matching foundation model ID; IAM grants access to that profile and constrains matching foundation-model resources to calls made through it. The previous implicit US profile default was removed because it could route customer inquiry data to an unchosen geography. This is template-level evidence only; staging invocation and data-routing validation are still required.
- Added `backend/scripts/validate_bedrock_profile.py`, a read-only deployment preflight that calls `GetInferenceProfile` and checks active system-defined profile status, source Region, profile/model pairing, and that every destination is in the customer-approved allowlist. Six standard-library unit tests cover the checks. This validates profile metadata only; it does not simulate IAM or invoke Bedrock.
- Replaced unkeyed SHA-256 contact indexes with HMAC-SHA-256 pseudonyms from a dedicated Secrets Manager key. API and worker roles can read only the configured secret. The API, webhook, and `/ready` require both the history-index and suppression migration markers to match the active key fingerprint. Backfills invalidate their markers before writing and restore them only after success, so interrupted or re-keyed migrations fail closed.
- Added inbound webhook tests for exact-byte HMAC verification, stale timestamps and invalid identifiers, bounded-body rejection, strict payload validation, event-to-lead mapping, atomic lead/audit/dedupe transactions, duplicate replay, and payload-mismatch rejection. These tests exposed and fixed silent acceptance of unexpected JSON fields (`extra="forbid"`) and whitespace-only names/messages.
- Added `backend/scripts/create_customer_index_secret.py`; Secrets Manager generates the 64-character key, and the CLI does not print the key or secret ARN. Retrieve the ARN through Secrets Manager when configuring the deployment. CodeQL initially flagged printing the ARN as clear-text secret logging; the CLI was changed to remove that output, and the next scan must confirm the finding closes. Automatic rotation is unsupported; re-keying requires a paused, marker-gated dual migration.
- Production repositories now propagate DynamoDB failures instead of pretending writes succeeded in process memory.
- Local/test demo mode now explicitly selects in-memory repositories even when AWS credentials are available, preventing accidental access to company tables; suppression migration checks are lazy rather than blocking app import/cold start.
- Agent credential detection no longer calls AWS STS during local analysis/tests; it checks whether a credential provider is configured and lets the bounded Bedrock invocation determine whether credentials are usable.
- CI now installs the locked backend/test dependencies and runs the backend behavioral suite; the health check assertion now matches the documented liveness response.
- Lead list/detail GET routes now compute current risk and opt-out presentation without writing the lead back. This prevents a read-triggered whole-record update from overwriting a concurrent operator change; memory-mode results are copied before projection as well.
- Production lead analysis now returns HTTP 503 when Bedrock is unavailable; heuristic analysis is limited to explicitly enabled demo mode.
- All `/api` routes now require an OIDC RS256 bearer token whose audience and tenant claim match deployment configuration. Unauthenticated local requests remain available only with explicit demo mode.
- OIDC access tokens must carry a configured API scope; OIDC and JWKS endpoints must use HTTPS.
- JWT validation requires expiration, issued-at and subject claims; a token with a different resource `aud` is rejected even if its `client_id` matches the browser app.
- Lead and follow-up writes and company-config changes are role-gated; audit entries now record the authenticated subject for those user actions.
- Lead and follow-up list routes now return at most 100 records per request with filter-bound continuation cursors; the browser loads additional pages on demand.
- Lead, follow-up, and audit repositories now query tenant-scoped GSIs; IDs from another tenant are treated as not found, and writes are conditioned on the configured tenant.
- Added [backend/scripts/backfill_tenant_data.py](backend/scripts/backfill_tenant_data.py) for a read-only-by-default, explicit per-customer legacy data migration.
- Added a signed inbound lead endpoint, Secrets Manager-backed HMAC verification, replay protection, and transactional idempotency; setup and sender protocol are in [INTEGRATIONS.md](INTEGRATIONS.md).
- Webhook idempotency markers now bind new event keys to the signed raw-body digest and return HTTP 409 when a key is reused with different content.
- Added cursor pagination to per-lead audit history and reduced new audit-event PII by omitting customer names and free-text follow-up notes.
- Replaced broad SAM DynamoDB CRUD grants with table/index-scoped permissions for the repository operations actually used by the API and signed webhook.
- Manual lead creation now writes the final lead state (including opt-out detection) and its initial audit event in one DynamoDB transaction.
- Response approval/edit/rejection now atomically update the lead and write all corresponding audit events; stale concurrent edits receive HTTP 409 instead of overwriting the newer lead.
- Successful rescue now atomically changes the lead, inserts its follow-up, and records the audit event; a competing update to that lead causes HTTP 409.
- Analysis now atomically saves the analyzed lead, any recommended follow-up, and its audit event; stale analysis results are rejected with HTTP 409.
- Lifecycle status changes now commit with their audit event, rejecting stale operator updates with HTTP 409.
- Follow-up completion/cancellation/blocking now commits the task and audit event in one DynamoDB transaction; a stale status change returns HTTP 409.
- SAM now enables point-in-time recovery on core data tables, applies bounded Lambda log retention, and defines Lambda error/throttle alarms with an optional SNS notification topic.
- Company business-rule updates now commit with their audit events in one transaction and use conditional writes to reject simultaneous administrator edits with HTTP 409.
- Server-side settings validation now constrains policy inputs and bounds extension JSON to 64 KB with DynamoDB-compatible numeric values; the settings form mirrors the same ranges.
- Database-backed FastAPI handlers now run as synchronous endpoints in FastAPI's worker thread pool; the signed webhook keeps async request-body streaming but moves blocking Secrets Manager and DynamoDB work to the thread pool. This prevents boto3 and Bedrock calls from blocking the ASGI event loop. The legacy `/analyze` endpoint remains request-bound, while the browser uses the durable job flow.
- Bedrock context now excludes customer email/phone, prior message summaries, lead identifiers, and arbitrary business-rule extensions; it includes only the inquiry, display name/source, a prior-interaction count, and selected company context. Agent output now rejects extra workflow fields and caps generated text/list sizes. This reduces unnecessary disclosure but does not replace customer-specific data-processing terms or retention controls.
- Lead analysis now has a durable `POST /api/leads/{lead_id}/analysis-jobs` flow with authenticated status polling, client idempotency keys, request/failure audit events, SQS retry/redrive, a separate least-privilege worker, job TTL, per-deployment concurrency cap, and queue-age/DLQ alarms. The worker records the job ID with the transactional lead update so a redelivered message after a successful write does not repeat the analysis workflow. The older synchronous endpoint is retained for compatibility.
- SAM now defines the API stage explicitly and applies configurable per-method throttling defaults (50 requests per second, burst 100), in addition to the Bedrock worker concurrency cap, to limit request floods and bound AI spend pressure per isolated customer stack.
- Lead, follow-up, audit, and business-configuration read endpoints now require the configured sales-operator or company-admin role, closing the prior gap where any scoped tenant token could read customer records.
- Response approval now records `APPROVED` and an explicit `delivery_status=not_configured` audit detail without marking the lead contacted. Only the new `SENT` response status suppresses SLA risk; legacy `SIMULATED_SENT` is treated as undelivered. Duplicate approval is idempotent, and a changed approved draft must be edited first. Historical leads may still retain a false `CONTACTED` lifecycle from the old behavior and require a reviewed correction plan.
- Added `backend/scripts/audit_legacy_response_states.py`, a tenant-index query-only, read-only report of legacy simulated/contacted records and related approval/lifecycle audit timestamps. It requires Query access on the tenant lead GSI and tenant-lead audit GSI, not table Scan or write access. It intentionally has no write option because the old approval event did not capture enough information to infer the prior lifecycle safely.
- The opt-out backfill now also finds and removes only the prior `tenant#<64 lowercase hex SHA-256>` suppression-key shape after rebuilding the keyed HMAC records. Dry-run reports the exact count; apply is tenant-conditioned and completes before the key-versioned readiness marker is restored.
- Backend direct dependencies are pinned to the versions present in the checked-in development environment; the unused `strands-agents-tools` package was removed from runtime requirements. Local frontend instructions now use `npm ci` with the checked-in lockfile.
- Backend `requirements.txt` now pins the complete Python 3.12 Linux x86_64 runtime dependency graph with package hashes; `requirements.in` is the direct-dependency source used to regenerate it with uv.
- Added `.github/workflows/quality.yml` to build the frontend, verify the pinned Python dependency graph remains reproducible, check backend syntax, and lint-validate the SAM template on pull requests and pushes to `main`.
- CI actions are pinned to official immutable SHAs for current Node 24-compatible releases, and both jobs use Ubuntu 24.04 instead of the floating `ubuntu-latest` image. The latest GitHub Actions run passed both jobs without runtime deprecation or runner-migration warnings. Branch protection, security/dependency scans, cloud integration tests, and staging deployment evidence remain open.
- SAM deployments now require an SNS alarm topic and route API/analysis-worker errors and throttles, queue age, and DLQ alarms to it; README documents subscriber verification before deployment.
- Added a tenant-scoped, company-admin-only lead record export covering related follow-ups and every audit page; the API records the export action and returns the JSON as a private, no-store download from the lead detail UI.
- Opt-outs now create durable tenant-scoped email/phone suppression keys. New leads, analysis, response approval, rescue, and lifecycle changes check those keys; state-changing DynamoDB transactions check or write suppression markers atomically to close race windows. Added a dry-run-first backfill script and a tenant migration marker; API/webhook operations fail closed until the marker exists.
- Customer email/phone lookup keys are HMAC-derived with a per-deployment Secrets Manager key. History and suppression migrations now record key fingerprints, and the API/webhook fail closed until both migration markers match the active key.

## Release gates still open

1. Validate browser sign-in/out, token expiry, provider role claims, and key rotation with the selected identity provider; decide refresh-token/session behavior.
2. Add the new DynamoDB GSIs, run the backfill dry run, apply it per isolated customer stack, and verify item/index counts before routing traffic.
3. Load-test tenant-scoped indexes, hot partitions, and end-to-end pagination, including audit history.
4. Reconcile existing leads that have `SIMULATED_SENT` and `CONTACTED` state from the old behavior; then connect and pilot the signed webhook against one actual company source and implement vendor-specific CRM sync and a real messaging provider with consent enforcement.
5. Configure the production alarm topic, add WAF/rate limits and encryption policy, and verify deployment/security/operations controls in a real AWS staging account.
6. Pause intake/outreach/worker processing, backfill customer-history indexes and existing opted-out contacts with the same HMAC key, then enable traffic only after both tenant migration markers match the active key.
7. Run security, integration, load, restore, and agent-quality validation using approved synthetic or customer-authorized data. The local benchmark is in `backend/evals/lead_agent_cases.json` and runs through `backend/scripts/evaluate_lead_agent.py --confirm-live-inference`; the flag authorizes potentially billed Bedrock requests. Expand and review its cases and thresholds before treating results as a release gate.
8. Run `backend/scripts/validate_bedrock_profile.py` with the complete customer-approved destination list, then validate the configured Bedrock profile/model pair with an actual bounded invocation in the target account/region; review IAM authorization and verify the approved destination geography against the customer's data-residency policy. The template now requires explicit geographic profile selection, but in-Region-only inference and global profiles are not supported by its current IAM configuration.
9. Build outcome reporting on a complete tenant-scoped dataset, with explicit reporting period, denominators, attribution rules, currency handling, and clear data-completeness status. Do not aggregate only the browser's currently loaded lead page or call operator-entered amounts incremental/recovered revenue without a customer-validated attribution method.

The source currently supports none of the claim that company data integrations are complete. Production account configuration and connector credentials would be needed to validate that claim.
